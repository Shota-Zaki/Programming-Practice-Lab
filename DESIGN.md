# DESIGN.md

## 状態

`確定`

Programming Practice Labの正式UI方向として、方向C「プロジェクト型」を採用する。

## 採用理由

- 学習開始時に「何を作れるようになるか」を明示できる。
- HTML、CSS、TypeScript、React、API、データベース、コンテナ、クラウドを一つの成果物に結び付けられる。
- 文法学習だけで終わらず、環境構築、実装、テスト、公開までを連続した経験として提供できる。
- 初学者には推奨順序を示しつつ、経験者は必要な工程から選択できる。
- 将来Japan Learning Labへ合流する場合も、プロジェクト単位を独立した学習コースとして移管しやすい。

## プロダクトの中心体験

利用者は、言語一覧ではなく作りたい成果物を選択する。

```text
プロジェクト選択
↓
必要技術と完成条件の確認
↓
短いレッスンとコード演習
↓
ローカル環境で実装
↓
テスト・改善
↓
クラウド公開
↓
成果物と学習履歴を保存
```

## 初期情報設計

### トップ

- 作れる成果物を最優先で表示する。
- 各プロジェクトに、対象技術、難易度、所要目安、実行方式を表示する。
- 前回の続きが存在する場合は、プロジェクト一覧より上で再開導線を表示する。
- 「基礎から学ぶ」は補助導線として残すが、主導線にはしない。

### プロジェクト工程

- 完成条件と最終成果物を冒頭に表示する。
- 工程を、準備、基礎実装、機能実装、品質確認、公開に分ける。
- 各工程に`browser`、`local`、`cloud`の実行方式を表示する。
- 前提未完了の工程は理由を表示し、単に操作不能にはしない。

### 演習

- 課題、コード、実行結果を意味的・視覚的に分離する。
- PCでは課題とエディタを並列表示し、結果を下部または右側に表示する。
- スマートフォンでは課題、コード、結果を段階表示する。
- 実行、採点、保存、失敗、再試行の状態を常に表示する。
- コードを入力する領域は、通常テキストや解説と明確に区別する。

## 初期プロジェクト

正式実装の最初の対象は「タスク管理画面」とする。

学習対象:

- HTML
- CSS
- JavaScript
- TypeScript
- React
- Git
- テスト基礎

後続候補:

- API付き商品管理
- Javaによる業務API
- PostgreSQLを使うデータ管理
- コンテナ化されたWebアプリ
- AWSへの公開

## ビジュアル方針

- 成果物カードと工程カードを中心にした中密度UIとする。
- 明るい背景に濃い文字を使用し、コード領域のみ暗い背景を許容する。
- 強調色は主要行動、進捗、状態の区別に限定する。
- 装飾よりも成果物、必要技術、現在位置を優先する。
- カードを過剰に入れ子にしない。
- 重要操作は色だけに依存せず、文言、形状、状態表示を併用する。

## レスポンシブ方針

### 375px

- 1列構成。
- ヘッダーの主要ナビは折りたたむ。
- コード演習はタブまたは縦方向の段階表示。
- 主要操作は画面下部に固定可能な構造とする。

### 768px

- プロジェクトカードは2列を基本とする。
- 工程一覧と概要を必要に応じて2列化する。
- 演習は課題とコードの2領域を切り替えられるようにする。

### 1280px

- プロジェクトカードは3列まで。
- 演習は課題、コード、結果を同時に確認できる構成を許容する。
- コンテンツ最大幅を設定し、横長画面で文章が広がりすぎないようにする。

## アクセシビリティ

- 主要操作はキーボードで到達・実行できること。
- フォーカス表示を消さないこと。
- ボタンとリンクの役割を混同しないこと。
- 状態変化は`aria-live`等で通知可能な構造にすること。
- コントラストを確保し、色だけで正否を表現しないこと。
- 動きを追加する場合は、動きを減らす設定を尊重すること。

## 技術的境界

- 教材データ、進捗保存、コード実行、認証を画面から分離する。
- URL基準パスを変更可能にし、将来`/programming`配下へ統合できるようにする。
- 進捗はRepositoryインターフェースを介し、初期はローカル保存、後に共通APIへ交換できる構造にする。
- ブラウザ実行、ローカル実行、クラウド実行を同一処理として扱わない。
- 任意コードを通常のWebサーバープロセスで直接実行しない。

## 不採用案の扱い

方向A「学習ガイド型」と方向B「開発環境型」は全体方針としては採用しない。

ただし、以下の要素は必要に応じて取り込む。

- 方向Aの明確な次行動と進捗表示
- 方向Bの演習画面におけるコード、テスト、出力の分離

これらはプロジェクト型の目的を補強する範囲で使用し、入口を言語一覧中心または開発ツール中心には変更しない。
